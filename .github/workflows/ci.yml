name: msvc

on:
  push:

jobs:
  install-msvc:
    runs-on: windows-2022
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup MSVC
        uses: ./.github/actions/setup-msvc
        with:
          vs-version: 14.43.17.13
          bootstrapper-url: https://download.visualstudio.microsoft.com/download/pr/9b2a4ec4-2233-4550-bb74-4e7facba2e03/00f873e49619fc73dedb5f577e52c1419d058b9bf2d0d3f6a09d4c05058c3f79/vs_BuildTools.exe

      # - name: Cache VS installation
      #   uses: actions/cache@v4
      #   with:
      #     path: |
      #       D:\vs-install
      #     key: сaching-key-0001

      # - name: Download and install VS Build Tools
      #   timeout-minutes: 30
      #   run: |
      #     if (-Not (Test-Path "D:\vs-install")) {
      #       curl -L -o vs_buildtools.exe https://download.visualstudio.microsoft.com/download/pr/165d4174-7d65-4baa-83d5-4652a3e56211/1906b0c92a4348c646f1849a2404b469a7e02edcf0cf76a05b972f7a445346e8/vs_BuildTools.exe

      #       $process = Start-Process -FilePath ".\vs_buildtools.exe" `
      #         -ArgumentList @(
      #           '--installPath', 'D:\vs-install',
      #           '--add', 'Microsoft.VisualStudio.Component.VC.14.40.17.10.x86.x64',
      #           '--add', 'Microsoft.VisualStudio.Component.VC.Tools.x86.x64',
      #           '--includeRecommended',
      #           '--noUpdateInstaller',
      #           '--quiet',
      #           '--norestart'
      #         ) `
      #         -NoNewWindow `
      #         -PassThru `
      #         -Wait

      #       Write-Host "Process exited with code: $($process.ExitCode)"
      #     }

      #     cmd /c "call D:\vs-install\VC\Auxiliary\Build\vcvars64.bat >nul && set" |
      #       ForEach-Object {
      #         if ($_ -match '^(.*?)=(.*)$') {
      #           "$($matches[1])=$($matches[2])" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
      #         }
      #       }

      # - name: Verify installation
      #   run: |
      #     if (Test-Path "D:\vs-install") {
      #       Write-Host "Installation directory exists"
      #       Get-ChildItem "D:\vs-install" | Select-Object Name
      #       if (Test-Path "D:\vs-install\VC\Tools\MSVC") {
      #         Write-Host "VCTools installed successfully"
      #         ls "D:\vs-install\VC\Tools\MSVC"
      #         if (Test-Path "D:\vs-install\VC\Tools\MSVC\14.40.33807") {
      #           Write-Host "14.40.33807 installed"
      #           & "D:\vs-install\VC\Tools\MSVC\14.40.33807\bin\Hostx64\x64\cl.exe"
      #         }
      #       } else {
      #         Write-Host "VCTools NOT found"
      #         exit 1
      #       }
      #     } else {
      #       Write-Host "Installation directory does NOT exist"
      #       exit 1
      #     }
      # cmd /c "D:\vs-install\VC\Auxiliary\Build\vcvars64.bat && powershell"
      # cmake .. -DK3TCHUP_CXX_STANDARD="detect"
      # cmd /c "call D:\vs-install\VC\Auxiliary\Build\vcvars64.bat && cmake .. -DK3TCHUP_CXX_STANDARD=detect"
      # cmd /c "call D:\vs-install\VC\Auxiliary\Build\vcvars64.bat >nul && set" |
      #   ForEach-Object {
      #     if ($_ -match '^(.*?)=(.*)$') {
      #       [System.Environment]::SetEnvironmentVariable($matches[1], $matches[2], 'Process')
      #     }
      #   }

      - name: Build the code
        run: |
          mkdir build
          cd build
          cmake .. -DK3TCHUP_CXX_STANDARD="detect"
          cmake --build .
